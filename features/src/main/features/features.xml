<?xml version="1.0" encoding="UTF-8"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.0.0" name="${project.artifactId}-${project.version}">
    <repository>mvn:org.apache.camel.karaf/apache-camel/${project.version}/xml/features</repository>

    <feature name="camel-tika" version="${project.version}" resolver="(obr)">
        <feature version="${project.version}">camel-core</feature>
        <bundle>wrap:mvn:junit/junit/${junit-version}</bundle>
        <bundle>mvn:org.apache.tika/tika-core/${tika-version}</bundle>
        <bundle>mvn:org.apache.tika/tika-bundle/${tika-version}</bundle>
        <bundle>mvn:org.apache.camel/camel-tika/${project.version}</bundle>
    </feature>

</features>


